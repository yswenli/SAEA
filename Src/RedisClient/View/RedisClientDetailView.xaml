<UserControl x:Class="RedisClient.RedisClientDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RedisClient"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <TextBox HorizontalAlignment="Stretch" IsReadOnly="True"  TextWrapping="Wrap" BorderThickness="0" BorderBrush="Transparent"  Text="{Binding ClientInfo,Mode=OneWay}"/>

        </ScrollViewer>
        

    </Grid>
</UserControl>
